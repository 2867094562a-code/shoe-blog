# V10.14 分类 / 标签选择栏修复

本版修复后台文章编辑区的分类和标签选择体验：

- 站点设置里的分类会同步显示到文章编辑区“从站点设置选择分类”。
- 站点设置里的标签会同步显示到文章编辑区“从站点设置选择标签”。
- 点击分类选项会直接设置文章分类。
- 点击标签选项会加入文章标签；再次点击会取消。
- 文章里输入 `@分类` 回车，仍会自动同步到站点设置。
- 文章里输入 `#标签` 回车，仍会自动同步到站点设置。
- 后端保存时继续做去重，避免重复同步两次。

更新后请在浏览器按 Ctrl + F5 强制刷新后台缓存。
